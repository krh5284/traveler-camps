baseURL: https://krh5284.github.io/traveler-camps/
locale: en-us
title: Traveler Camps
theme: blowfish

    
caches:
  assets:
    dir: :resourceDir/_gen
    maxAge: -1
  getresource:
    dir: :cacheDir/:project
    maxAge: -1
  images:
    dir: :cacheDir/images
    maxAge: -1
  misc:
    dir: :cacheDir/:project
    maxAge: -1
  modulegitinfo:
    dir: :cacheDir/modules
    maxAge: 24h
  modulequeries:
    dir: :cacheDir/modules
    maxAge: 24h
  modules:
    dir: :cacheDir/modules
    maxAge: -1

services:
  disqus:
    shortname: kelliebernhardt
