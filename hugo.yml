baseURL: https://krh5284.github.io/traveler-camps/
locale: en-us
title: Traveler Camps
theme: hugo-PaperMod

menu:
  main:
    - identifier: map
      name: Map
      url: /map/
      weight: 10

    - identifier: new-job-site
      name: New Job Site Request
      url: /new-job-site/
      weight: 20
      
    - identifier: new-campgroud
      name: New Campground Request
      url: /new-campground/
      weight: 30

    - identifier: contact-us
      name: Contact Us
      url: /contact-us/
      weight: 40

    
caches:
  assets:
    dir: :resourceDir/_gen
    maxAge: -1
  getresource:
    dir: :cacheDir/:project
    maxAge: -1
  images:
    dir: :cacheDir/images
    maxAge: -1
  misc:
    dir: :cacheDir/:project
    maxAge: -1
  modulegitinfo:
    dir: :cacheDir/modules
    maxAge: 24h
  modulequeries:
    dir: :cacheDir/modules
    maxAge: 24h
  modules:
    dir: :cacheDir/modules
    maxAge: -1


params:
  homeInfoParams:
    Title: Welcome to Traveler Camps
    Content: >
      The goal of this site is to provide one location where traveling workers
      can find campgrounds and RV parks near their job site.
      Our site is new and our map is incomplete. Please help us add to the map by following the links at the top right of the page.
      Go to the [Map](https://krh5284.github.io/traveler-camps/map/)
